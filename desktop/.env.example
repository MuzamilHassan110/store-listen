# Code signing (production). Never commit the PFX or password.
# CSC_LINK=D:\certs\storelisten.pfx
# CSC_KEY_PASSWORD=
# WIN_CSC_LINK=
# WIN_CSC_KEY_PASSWORD=

# Built into the app as the default API. Users can override this in the first-run wizard.
VITE_BACKEND_URL=http://localhost:3000
